export class Student {
  constructor(id, firstName, lastName, studentId, email, createdDate) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.studentId = studentId;
    this.email = email;
    this.createdDate = createdDate;
  }
}
